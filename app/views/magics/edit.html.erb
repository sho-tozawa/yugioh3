<%= form_for(@magic, url: magic_path(@maic)) do |form| %>

  <% if @magic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@magic.errors.count, "error") %> prohibited this user from being saved:</h2>
      <ul>
        <% @magic.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>