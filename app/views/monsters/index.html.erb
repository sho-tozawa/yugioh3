<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Title</title>
</head>
<body>

<% @monsters.each do |monster| %>
  <%= link_to(monster.name,"/monsters/#{monster.id}") %>
  <%= monster.level %>
<% end %>

</body>
</html>